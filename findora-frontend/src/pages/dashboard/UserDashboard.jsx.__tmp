import DashboardLayout from "../../layouts/DashboardLayout";

function UserDashboard() {
  return (
    <DashboardLayout>
      <div className="mx-auto max-w-6xl space-y-8">
        <div className="flex items-start gap-6">
          <div className="flex-1 rounded-4xl bg-white p-6 shadow-xl">
            <p className="text-sm font-semibold uppercase tracking-[0.24em] text-blue-700">Dashboard Overview</p>
            <h1 className="mt-4 text-3xl font-bold text-slate-900">Welcome back, Duvindu!</h1>
            <p className="mt-3 text-sm text-slate-600">You have 3 new matches for your lost items. Let's get them back to you.</p>

            <div className="mt-6 flex items-center gap-4 rounded-2xl bg-blue-50 p-4">
              <div className="h-10 w-10 rounded-full bg-white flex items-center justify-center text-blue-600">✓</div>
              <div>
                <p className="text-sm font-semibold">Recovery Success!</p>
                <p className="text-sm text-slate-600">Your Sony Headphones were returned.</p>
              </div>
            </div>

            <div className="mt-6 grid gap-6 md:grid-cols-2">
              <div className="space-y-3">
                <h4 className="text-sm font-semibold text-slate-700">Recent Activity</h4>
                <div className="mt-2 space-y-4">
                  <div className="rounded-2xl bg-white p-4 shadow">
                    <p className="text-xs text-blue-600">Today, 10:45 AM</p>
                    <p className="mt-1 text-sm font-semibold">New Match Found</p>
                    <p className="text-sm text-slate-500 mt-1">A strong match for your "Blue Backpack" in the Transit Hub.</p>
                  </div>

                  <div className="rounded-2xl bg-white p-4 shadow">
                    <p className="text-xs text-purple-600">Yesterday, 4:20 PM</p>
                    <p className="mt-1 text-sm font-semibold">Claim Accepted</p>
                    <p className="text-sm text-slate-500 mt-1">The finder of your "House Keys" has verified your claim.</p>
                  </div>

                  <div className="rounded-2xl bg-white p-4 shadow">
                    <p className="text-xs text-slate-500">Mar 10, 2024</p>
                    <p className="mt-1 text-sm font-semibold">Post Created</p>
                    <p className="text-sm text-slate-500 mt-1">You reported a "Silver Ring" found in Golden Gate Park.</p>
                  </div>
                </div>
              </div>

              <div>
                <h4 className="text-sm font-semibold text-slate-700">My Recent Posts</h4>
                <div className="mt-3 grid grid-cols-3 gap-3">
                  <div className="rounded-2xl bg-white p-4 shadow text-center">
                    <div className="h-16 w-16 mx-auto rounded-md bg-slate-100" />
                    <p className="mt-3 text-sm font-semibold">Blue Backpack</p>
                    <p className="text-xs text-slate-500">Reported: Oct 14, 2023</p>
                  </div>

                  <div className="rounded-2xl bg-white p-4 shadow text-center">
                    <div className="h-16 w-16 mx-auto rounded-md bg-slate-100" />
                    <p className="mt-3 text-sm font-semibold">Car Keys</p>
                    <p className="text-xs text-slate-500">Reported: Oct 12, 2023</p>
                  </div>

                  <button className="rounded-2xl border-2 border-dashed border-slate-200 p-4 flex items-center justify-center">+
                  </button>
                </div>
              </div>
            </div>
          </div>

          <aside className="w-72">
            <div className="rounded-4xl bg-white p-6 shadow-xl">
              <h4 className="text-sm font-semibold text-slate-700">Profile Strength</h4>
              <p className="mt-2 text-2xl font-bold text-blue-600">85%</p>
              <p className="mt-3 text-sm text-slate-500">Complete your identity verification to increase your trust score among the Findora community.</p>
              <button className="mt-4 rounded-full bg-blue-600 px-4 py-2 text-white">Verify Identity Now</button>
            </div>
          </aside>
        </div>
      </div>
    </DashboardLayout>
  );
}

export default UserDashboard;
