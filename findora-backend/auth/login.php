<?php
$origin = $_SERVER['HTTP_ORIGIN'] ?? ''; if ($origin === 'https://findora.freehosting.dev' || $origin === 'http://localhost:5173') { header('Access-Control-Allow-Origin: ' . $origin); } header('Access-Control-Allow-Headers: Content-Type, X-CSRF-Token'); header('Access-Control-Allow-Methods: POST, OPTIONS'); header('Content-Type: application/json');
header("Access-Control-Allow-Methods: POST, OPTIONS");
header("Content-Type: application/json");

if ($_SERVER["REQUEST_METHOD"] === "OPTIONS") {
require_once __DIR__ . '/../config/db.php'; require_once __DIR__ . '/../classes/Repositories/UserRepository.php'; require_once __DIR__ . '/../classes/Services/AuthService.php'; if ($_SERVER['REQUEST_METHOD'] !== 'POST') { http_response_code(405); echo json_encode(array('status' => 'error', 'message' => 'Only POST method is allowed')); exit(); } $data = json_decode(file_get_contents('php://input'), true); if (!is_array($data)) { http_response_code(400); echo json_encode(array('status' => 'error', 'message' => 'Invalid JSON input')); exit(); } $loginId = isset($data['login_id']) ? trim($data['login_id']) : ''; $password = isset($data['password']) ? (string) $data['password'] : ''; if ($loginId === '' || $password === '') { http_response_code(400); echo json_encode(array('status' => 'error', 'message' => 'NIC/Email and password are required')); exit(); } try { $authService = new AuthService(new UserRepository($conn)); $user = $authService->authenticate($loginId, $password); if ($user === null) { http_response_code(401); echo json_encode(array('status' => 'error', 'message' => 'Invalid NIC/Email or password')); exit(); } SessionManager::login($user); $safeUser = SessionManager::user(); echo json_encode(array('status' => 'success', 'message' => 'Login successful', 'redirect_url' => $authService->getRedirectUrl($safeUser['role']), 'csrf_token' => SessionManager::csrfToken(), 'user' => $safeUser)); } catch (Throwable $error) { error_log('login.php: ' . $error->getMessage()); http_response_code(500); echo json_encode(array('status' => 'error', 'message' => 'Login service is temporarily unavailable')); } exit();
